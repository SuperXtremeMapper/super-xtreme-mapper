# Assistant polish, evaluation and JSON repair

User approved completing three items then pausing: Assistant UI/UX consistency, live evaluation, and AI-assisted JSON repair. No additional profiles or visual controller editor. No Agency required. Keep existing work and documents safe; locally integrate, no remote push.

## UI acceptance
Match AppThemeV2: compact system typography, stone surfaces, amber primary actions, dark native appearance. Floating document-bound window retained. Conversation occupies the main area; composer remains anchored. Connection settings and MIDI setup are expandable, with visible status and discoverable entry points. Voice is off initially, speech enters editable composer, Send is explicit. Preserve current selection binding, locked/stale guards, source links, local guides and precise review/Undo. Show source/review details clearly; avoid exposing IDs until details needed. Test at normal and minimum sizes.

## Evaluation acceptance
Evaluate actual app against synthetic mapping fixtures using existing stored Anthropic key if accessible, without printing credentials. Limit representative paid requests to explanations, follow-ups, reviewed edits and ambiguous creation/repair. No user mapping uploads necessary. Evaluate permission/error/cancel states. Real speech and physical MIDI require available microphone/user speech/controller; report unavailable checks honestly and do all available work without blocking repair.

## Repair acceptance
Keep exact original bytes immutable and never overwrite the input file. Failed import offers optional AI repair with explicit disclosure/opt-in and existing stored key/Sonnet. Model only proposes bounded exact textual edits, with locally computed before/after review. Never execute model tools. Do not send or permit alteration of preservation payloads; for malformed data whose sensitive regions cannot be safely isolated, refuse AI repair with an actionable local instruction. Syntax-invalid source-free JSON and syntactically valid schema-invalid exports are representative supported cases. Revalidate with JSONImportService and preservation writer before repaired import is enabled. Applying a proposal changes the review candidate only; Import explicitly opens a new untitled document. Discard restores original review. Stale/cancelled responses do nothing. Exact patches must reject invalid/overlapping/ambiguous targets, oversized responses, no-ops and protected-region edits. No guesses that bypass catalogue validation. Bounded requests, sanitized network errors, independent injectable transport/coordinator tests. Files exceeding AI repair bounds retain existing local diagnostics/import behavior.
